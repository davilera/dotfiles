# Basic settings
# --------------
unset MAILCHECK
PATH="./node_modules/.bin:./vendor/bin:$HOME/.config/composer/vendor/bin:$HOME/.local/bin:$HOME/.cargo/bin:$PATH"

# Faster navigation
# -----------------
# eval "$(fasd --init auto)"
# alias j="fasd_cd -d"
eval "$(zoxide init --cmd j zsh)"
alias cd="j"

# Kitty
alias kitty="LIBGL_ALWAYS_SOFTWARE=true GALLIUM_DRIVER=llvmpipe kitty"

# NVM
# ---
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" --no-use
# nvm use 16 is too slow. Use this instead (with --no-use):
PATH="${NVM_DIR}/versions/node/v16.20.1/bin:${PATH}"

# Brew
# ----
if [ -d /home/linuxbrew/.linuxbew ];
then
	eval $(/home/linuxbrew/.linuxbrew/bin/brew shellenv)
fi

# Haskell
# ------------
[ -f "/Users/david/.ghcup/env" ] && source "/Users/david/.ghcup/env"

# Aliases
# ------------
wp() {
	[ ! -e .lando.yml ] && echo "This doesn't look like a WordPress installation. Bye!" >&2 && return
	lando wp $@ --path=`\grep webroot .lando.yml | cut -d: -f2 | xargs`
}

wpd() {
	project=`basename $PWD`
	dir="$HOME/Programs/dev/wordpress.org/$project/trunk"
	echo "Meld with “$dir”"
	[[ -d "$dir" ]] && meld "$dir" . || echo "SVN project “$project” not found."
}

alias ovim="/usr/bin/vim"
alias vi="lvim"
alias vim="lvim"
alias cat="bat --tabs 2 --theme='Catppuccin Mocha'"
alias mdcat="bat -l markdown --tabs 2 --theme='Catppuccin Mocha'"
alias colorize="ccze -A"
# alias grep="grep --color --exclude-dir=vendor --exclude-dir=node_modules --exclude-dir=e2e-tests --exclude-dir=build --exclude-dir=dist --exclude-dir=build-module --exclude=\"*.min.*\" --exclude=\"*.map\" --exclude-dir=\".git\""
alias grep="ag --norecurse --noheading --nobreak --ignore node_modules --ignore vendor --ignore e2e-tests --ignore build --ignore dist --ignore build-module --ignore .lando"
alias top="btop"
alias htop="btop"
# alias tree="tree -C"
alias tree="lsd --group-dirs=first --tree"

alias ga="git add"
alias gc="git commit"
alias gco="git checkout"
alias gd="git difftool --no-symlinks --dir-diff"
alias gg="git pull"
alias gp="git push"
alias gs="git status"
alias gl="git blame"
alias lg="lazygit"
alias lz="lazygit"

alias yi="yarn install"
alias yr="yarn run"
alias ry="yarn run"

alias ping="~/.local/bin/prettyping"
alias pip="pip3"

if [ -d "/Users" ]
then
	export LSCOLORS="ExGxBxDxCxEgEdxbxgxcxd"
	alias ls="ls -G"
	alias eog="open -a Firefox"
else
	# alias ls="ls --color --group-directories-first"
	alias ls="lsd --group-dirs=first"
	alias eog="xviewer"
fi

if [ "$TERM" = 'xterm-kitty' ];
then
	alias eog="kitty +kitten icat"
	alias icat="kitty +kitten icat"
fi

if [ -e /proc/version ] && [ `grep microsoft /proc/version | wc -l` -gt 0 ];
then
	cd ~
fi
